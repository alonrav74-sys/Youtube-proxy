{
  "name": "youtube-proxy",
  "version": "1.0.0",
  "description": "YouTube proxy with Whisper transcription",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
